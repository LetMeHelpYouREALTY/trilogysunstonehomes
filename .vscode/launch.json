{
    "configurations": [
        {
            "name": "Containers: Node.js Launch",
            "type": "docker",
            "request": "launch",
            "preLaunchTask": "docker-run: debug",
            "platform": "node",
            "node": {
                "package": "${workspaceFolder}/archive/spiffy-saturn/package.json",
                "localRoot": "${workspaceFolder}/archive/spiffy-saturn"
            }
        }
    ]
}