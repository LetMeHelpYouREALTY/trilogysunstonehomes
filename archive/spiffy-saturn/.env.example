# Optional: Google Search Console verification (HTML tag method).
# Get the value from GSC → Settings → Ownership verification → HTML tag.
# Add only the content value (the long string), not the full meta tag.
# Set in Vercel: Project → Settings → Environment Variables.
PUBLIC_GSC_VERIFICATION=
