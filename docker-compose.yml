services:
  spiffysaturn:
    image: spiffysaturn
    build:
      context: archive/spiffy-saturn
      dockerfile: ./Dockerfile
    environment:
      NODE_ENV: production
    ports:
      - 3000:3000
